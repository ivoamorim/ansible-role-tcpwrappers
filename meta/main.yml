---
galaxy_info:
  role_name: tcpwrappers
  author: Ivo Amorim
  description: Manages entries in `/etc/hosts.allow` and `/etc/hosts.deny` which are used by TCP Wrappers to limit connections to daemons that utilise the `libwrap` library.
  license: BSD
  min_ansible_version: 1.0
  platforms:
    - name: Debian
      versions:
        - stretch
    - name: EL
      versions:
        - 7
    - name: Ubuntu
      versions:
        - bionic
  categories:
    - system
    - security
    - tcpwrappers
dependencies: []
